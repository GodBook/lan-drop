# LAN Drop 1.5.0

## 主要更新

- Android 主代码正式纳入 Git，移除仓库中的公开签名密钥，发布签名改由 GitHub Actions Secrets 提供。
- 扫码 PIN 使用一次后通过 `303` 跳转清理地址栏，改用随机会话 Cookie，并统一失败限流和 `Referrer-Policy: no-referrer`。
- 上传接口增加 5 MiB 分片限制、总分片数/总文件大小限制、磁盘余量检查、multipart 内存阈值和并发请求闸门。
- 上传支持暂停、继续、取消，以及失败分片自动重试；合并过程支持取消和原子回滚。
- 二维码明确限制为 Medium、Version 1–5；超出容量显式失败，并加入独立扫码器真实解码测试。
- Windows 桌面端增加单实例、系统托盘、最小化到托盘、打开接收目录、复制连接地址、存储目录切换和多网卡切换。
- Windows 桌面端和命令行 EXE 使用统一的 LAN Drop 专属应用图标。
- 文件列表支持搜索、类型筛选、分页、全选本页和批量删除；目录索引缓存减少重复扫描。
- WebSocket 改为指数退避加随机抖动，并响应浏览器在线/离线状态。
- CLI/桌面端接入 mDNS/Bonjour，Android 使用 NSD 自动发现；二维码继续作为可靠兜底。

## 升级注意

旧版 Android 签名密钥已经公开，1.5.0 使用全新密钥发布。旧签名 APK 无法直接覆盖安装，升级前需要卸载旧版；请先备份应用内需要保留的数据。

正式发布前必须在仓库 Secrets 配置：

- `ANDROID_KEYSTORE_BASE64`
- `ANDROID_KEYSTORE_PASSWORD`
- `ANDROID_KEY_ALIAS`
- `ANDROID_KEY_PASSWORD`

本版本通过 Go 测试、`go vet`、桌面模块测试、前端语法检查、二维码独立解码测试和浏览器回归验证。
